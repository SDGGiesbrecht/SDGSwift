🇬🇧EN
No compatible version of Swift could be located. (5.0.0 ≤ x ≤ 5.0.1)
It must be installed and locatable with one of the following commands:
$ which swift
$ xcrun --find swift
$ swiftenv which swift

🇺🇸EN
No compatible version of Swift could be located. (5.0.0 ≤ x ≤ 5.0.1)
It must be installed and locatable with one of the following commands:
$ which swift
$ xcrun --find swift
$ swiftenv which swift

🇨🇦EN
No compatible version of Swift could be located. (5.0.0 ≤ x ≤ 5.0.1)
It must be installed and locatable with one of the following commands:
$ which swift
$ xcrun --find swift
$ swiftenv which swift

🇩🇪DE
Keine passende Version von Swift wurde gefunden. (5.0.0 ≤ x ≤ 5.0.1)
Es muss installiert und unter einem der folgenden Befehle vorhanden sein:
$ which swift
$ xcrun --find swift
$ swiftenv which swift

