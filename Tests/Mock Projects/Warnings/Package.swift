// swift-tools-version:5.0

import PackageDescription

let package = Package(
    name: "Warnings",
    products: [
        .library(
            name: "Warnings",
            targets: ["Warnings"]),
    ],
    targets: [
        .target(
            name: "Warnings",
            dependencies: []),
    ]
)
