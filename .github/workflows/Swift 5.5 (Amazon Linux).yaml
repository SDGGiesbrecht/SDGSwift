# Swift 5.5 (Amazon Linux).yaml
#
# This source file is part of the SDGSwift open source project.
# https://sdggiesbrecht.github.io/SDGSwift
#
# Copyright ©2021–2022 Jeremy David Giesbrecht and the SDGSwift project contributors.
#
# Soli Deo gloria.
#
# Licensed under the Apache Licence, Version 2.0.
# See http://www.apache.org/licenses/LICENSE-2.0 for licence information.

name: Swift 5.5 (Amazon Linux)

on: [push, pull_request]

jobs:
  Amazon_Linux:
    name: Swift 5.5 (Amazon Linux)
    runs-on: ubuntu-20.04
    container: swift:5.5.1-amazonlinux2
    steps:
    - name: Check out
      uses: actions/checkout@v2
    - name: Cache Workspace
      uses: actions/cache@v2
      with:
        key: Amazon‐Linux‐${{ hashFiles('.github/workflows/**') }}
        path: .build/SDG/Workspace
    - name: Install SwiftPM dependencies
      shell: bash
      run: |
        set -x
        yum install --assumeyes \
          ncurses-devel \
          sqlite-devel
    - name: Install Workspace dependencies
      shell: bash
      run: |
        set -x
        yum install --assumeyes \
          curl
    - name: Validate
      shell: bash
      run: |
        set -x
        swift test
    - name: Grant permission to cache
      shell: bash
      run: |
        set -x
        chmod -R a+rwx .
